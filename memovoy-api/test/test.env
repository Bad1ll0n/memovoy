# Valores falsos, só para os módulos poderem ser importados nos testes unitários.
# A chave da Groq já não é necessária: o cliente do LLM é construído a pedido.
# Nada aqui liga a serviços reais: os testes não fazem rede nem tocam na BD.
DATABASE_URL=postgresql://test:test@localhost:5432/test_nao_usada
JWT_SECRET=segredo-falso-para-testes
JWT_REFRESH_SECRET=segredo-refresh-falso-para-testes
NODE_ENV=test
