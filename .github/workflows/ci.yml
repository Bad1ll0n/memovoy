name: CI

on:
  push:
    branches: [main, develop]
  pull_request:

# Cancela runs antigos do mesmo branch quando chega um push novo.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  api:
    name: API — testes
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: memovoy-api
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: memovoy-api/package-lock.json

      - run: npm ci

      # Testes unitários: não tocam na base de dados nem fazem rede.
      # As variáveis falsas vivem em test/test.env.
      - run: npm test

  api-integration:
    name: API — integração
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: memovoy-api

    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: memovoy_test
        ports:
          - 5432:5432
        # Sem isto os passos arrancam antes de o Postgres aceitar ligações.
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: memovoy-api/package-lock.json

      - run: npm ci

      # As credenciais do serviço acima coincidem com as de test/integration.env,
      # por isso não é preciso passar DATABASE_URL aqui.
      - name: Aplicar migrations
        run: npm run test:integration:setup

      - name: Testes de integração
        run: npm run test:integration

  web:
    name: Web — testes, typecheck e build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: memovoy-web
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: memovoy-web/package-lock.json

      - run: npm ci

      - name: Testes
        run: npm test

      - name: Typecheck
        run: npx tsc --noEmit

      - name: Build
        run: npm run build
        env:
          NEXT_PUBLIC_API_URL: http://localhost:4000

  lint:
    name: Web — lint (informativo)
    runs-on: ubuntu-latest
    # Não bloqueia: há 25 erros e 23 avisos por resolver do ESLint 9 /
    # React 19. Passar isto a bloqueante assim que a dívida estiver saldada.
    continue-on-error: true
    defaults:
      run:
        working-directory: memovoy-web
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: memovoy-web/package-lock.json

      - run: npm ci

      - run: npm run lint
