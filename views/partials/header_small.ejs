<nav class="navbar navbar-expand-lg navbar-light">
        <div class="container">
            <a class="navbar-brand me-auto" href="/dashboard">
                <img src="/assets/images/Insight_Logo.png" alt="Insight Logo" style="height: 120px; width: auto; object-fit: contain;">
            </a>

            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav mx-auto">
                    <li class="nav-item">
                        <a href="/dashboard" class="nav-link">
                            <span class="icon">🏠</span> Home
                        </a>
                    </li>

                    <li class="nav-item">
                        <button class="nav-link btn-reset border-0 bg-transparent" data-bs-toggle="modal" data-bs-target="#createItineraryModal">
                            <span class="icon">➕</span> New Trip
                        </button>
                    </li>

                    <li class="nav-item">
                        <button class="nav-link btn-reset border-0 bg-transparent" data-bs-toggle="modal" data-bs-target="#sharePhotoModal">
                            <span class="icon">📸</span> New Post
                        </button>
                    </li>

                    <li class="nav-item">
                        <a href="/profile_trips" class="nav-link">
                            <span class="icon">❤️</span> My Trips
                        </a>
                    </li>
                </ul>

                <ul class="navbar-nav ms-auto">
                    <% if (user) { %>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" id="userDropdown" role="button"
                            data-bs-toggle="dropdown" aria-expanded="false">
                            <img src="/assets/images/avatar.jpg" alt="Avatar" class="rounded-circle" width="35" height="35">
                            <span class="ms-2">
                                <%= user.first_name %>
                            </span>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown">
                            <li><a class="dropdown-item" href="/profile"><span class="icon">👤</span> Perfil</a></li>
                            <li><a class="dropdown-item" href="/settings"><span class="icon">⚙️</span> Definições</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item text-danger" href="/users/logout">Sair</a></li>
                        </ul>
                    </li>
                    <% } else { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/users/login">Login</a>
                    </li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>